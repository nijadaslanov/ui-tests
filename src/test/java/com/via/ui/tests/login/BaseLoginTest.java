package com.via.ui.tests.login;

import com.via.ui.base.BaseTest;
import org.testng.annotations.BeforeMethod;

import static com.via.ui.pages.login.LoginPage.*;
import static org.testng.Assert.assertEquals;
import static org.testng.Assert.assertTrue;

public class BaseLoginTest extends BaseTest {

//
//    @Override
//    public void waitForPageToLoad() {
//        assertTrue(loginPage.get()
//                           .getPageElement(EMAIL_INPUT)
//                           .isVisible(), "The LoginPage -> email input was not loaded");
//        assertTrue(loginPage.get()
//                           .getPageElement(PASSWORD_INPUT)
//                           .isVisible(), "The LoginPage -> password input was not loaded");
//        assertTrue(loginPage.get()
//                           .getPageElement(SUBMIT_BUTTON)
//                           .isVisible(), "The LoginPage -> submit button was not loaded");
//        assertTrue(loginPage.get()
//                           .getPageElement(FORGOT_PASSWORD_LINK)
//                           .isVisible(), "The LoginPage -> forgot password link was not loaded");
//        // We want this to be the last assert/check because 'getPageElement' knows how to intelligently
//        // wait for an element, whereas waiting for a new page to load is a bit trickier.
//        verifyURL();
//    }
//
//    @Override
//    public void verifyURL() {
//        assertEquals(loginPage.get()
//                             .getCurrentUrl(), loginPage.get().url, "LoginPage url does not match actual url.");
//    }
    }

